.container
  %h1= "Liste des blogs"  
  %table.table
    %thead
      %tr
        %th
          Titre du blog
        %th
          %span.pull-right= "Actions"
    %tbody
      - @blogs.each do |blog|
        %tr
          %td
            = blog.title
          %td
            = link_to "Supprimer", admin_blog_path(blog), method: :delete, class: "btn btn-danger pull-right"
            = link_to "Edit", edit_admin_blog_path(blog), class: "btn btn-primary pull-right"
